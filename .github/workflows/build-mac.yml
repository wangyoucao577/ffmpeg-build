name: Build - mac
on: 
  push:
    branches:
      - '**'
    tags:
      - 'v*'
    paths:
    - '!buildtools/docker/**'
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: '0'
          submodules: 'true'

      - name: Install tools
        run: |
          brew install tree bear 

      - name: Install yasm
        run: |
          brew install yasm

      - name: Build
        run: |
          buildtools/scripts/build.sh
          tree build

      - uses: actions/upload-artifact@v3
        with:
          name: ffmpeg-deps-bin
          path: build/bin/
